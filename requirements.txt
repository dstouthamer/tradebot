# ---- AI Boekhouder: Python dependencies ----
# Core MVP (kept lean so the system runs out-of-the-box, keyless).
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
httpx==0.27.0
python-dotenv==1.0.1
python-multipart==0.0.9

# Dashboard (MVP UI).
streamlit==1.36.0
pandas==2.2.2

# ---- Optional integration extras (enable as you wire real services) ----
# Real receipt OCR. Requires the system `tesseract` binary + Pillow.
# pytesseract==0.3.10
# Pillow==10.3.0
# Tighter MT940 parsing (we ship a stdlib fallback parser too).
# mt-940==4.30.0
# Anthropic SDK for the ClaudeLLM seam (claude-opus-4-8).
# anthropic==0.34.0
